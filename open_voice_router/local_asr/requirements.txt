# Base dependencies for the vendored local ASR sidecar (HTTP layer + audio IO).
# Engine-specific packages (onnx-asr, faster-whisper, sherpa-onnx, ...) are
# installed on demand by LocalSTTManager from the model registry's
# pip_requirements — see open_voice_router/local_asr/registry.py.
flask>=3.0.0
waitress>=3.0.0
psutil>=5.9.0
numpy>=1.24.0
werkzeug>=3.0.0
huggingface-hub>=0.23.0
